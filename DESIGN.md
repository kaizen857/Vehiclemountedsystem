# 智能车载系统课程项目设计文档

## 1. 项目背景与当前仓库基础

本项目定位为安卓课程结课作业：在普通 Android 手机上模拟“智能车机系统”。由于无法接入真实车辆 CAN 总线或车载 ECU，系统以手机自带传感器、手机电池信息、Android 系统媒体会话和本地演示状态作为基础数据来源，红外空调控制作为可选扩展能力，重点展示车载横屏界面、车辆状态可视化、控制器交互和硬件能力降级处理。

当前仓库仍是 Android Studio 生成的基础单模块项目，适合作为课程项目从零扩展：

- 只有 `:app` 一个模块，包名与命名空间为 `com.example.vehicle_mountedsystem`。
- 当前技术栈是 Java + XML View 系统，入口为 `app/src/main/java/com/example/vehicle_mountedsystem/MainActivity.java`。
- 当前 Activity 继承 `AppCompatActivity`，布局文件为 `app/src/main/res/layout/activity_main.xml`，根布局是 `ConstraintLayout`。
- 当前依赖包含 `appcompat`、`material`、`constraintlayout`、`activity-ktx`、JUnit、AndroidX Test、Espresso；暂未引入 Compose、Navigation、ViewModel、Room、地图 SDK 或媒体播放库。
- 当前 `AndroidManifest.xml` 只声明启动 Activity，尚未声明定位、网络、红外、通知等权限。
- 当前本地测试与 instrumentation 测试仍是模板测试，需要在实现业务逻辑时补充真实测试。

因此，本设计文档采用“单模块、Java/XML、轻量分层”的方案，不假设已有 Compose、多模块或复杂框架。

## 2. 项目目标与非目标

### 2.1 项目目标

1. 构建横屏智能车机界面，整体风格偏冷色调、科技感、简洁克制。
2. 展示车辆状态信息：时速、档位、电量、电机功率、转速/功率类指标、车况信息、导航摘要、空调状态、G 值、陀螺仪等。
3. 提供车载控制演示：多媒体控制、空调控制、车窗控制、后视镜调节、座椅加热/通风/按摩等。
4. 使用左侧标签栏进行页面切换，避免所有组件堆在同一个页面。
5. 提供“简洁车辆信息页”，只显示车辆信息相关内容，不显示控制器或多媒体组件。
6. 页面切换、控件状态变化、数值变化需要有动画反馈。
7. 使用真实手机能力作为主要数据来源，并在硬件或权限不可用时在对应组件下方显示不可用说明。

### 2.2 非目标

1. 不实现真实车辆 CAN 总线、OBD、ECU 或车载硬件控制。
2. 不把本项目设计成生产级车载安全系统。
3. 不接入真实地图 SDK，也不接入 Google Maps、高德地图或百度地图。
4. 默认不实现完整实时导航、路线重规划、语音播报和车道级导航。
5. 不额外实现独立播放器作为主要方案；多媒体页优先联动系统已有媒体会话。
6. 默认不迁移到 Jetpack Compose，避免偏离当前 Java/XML 项目基础。

## 3. 推荐课程项目范围

### 3.1 基础版本范围

基础版本应优先完成以下能力：

- 横屏主界面框架：左侧标签栏 + 右侧内容区。
- 总览页：速度、电量、电机功率、档位、G 值、陀螺仪摘要、导航摘要、空调摘要。
- 简洁车辆信息页：只展示车辆状态、车况、速度、档位、电量、功率、传感器状态。
- 传感器页：展示加速度计与陀螺仪三轴数据、G 值计算结果、传感器可用性。
- 导航页：使用风格化导航占位图或自绘路线卡片，不接入真实地图，展示目的地模拟、速度、路线进度。
- 空调页：本地空调状态机、温度、风量、模式、内外循环、AC 开关，并持久保存状态；红外真实控制作为扩展。
- 多媒体页：联动系统当前媒体会话，支持播放/暂停、上一首、下一首、曲目信息和进度展示；没有可控会话时显示授权或不可用说明。
- 控制页：车窗、后视镜、座椅加热/通风/按摩等演示控件。
- 设置页：通知监听授权状态、传感器可用性、红外可用性、数据源说明。

### 3.2 可选扩展范围

如果基础版本完成后仍有时间，可选择以下扩展：

- 接入 `ConsumerIrManager`，在测试手机上用真实红外控制可测试空调。
- 增加更多空调品牌/型号的红外码库。
- 增加红外发送日志、最近一次发送命令和失败重试提示。
- 增加 IMU 速度估算曲线、漂移校准和重置按钮。
- 引入 AndroidX Lifecycle/ViewModel，使跨页面共享状态更规范。
- 增加传感器数据曲线图或历史记录，但不作为第一优先级。

## 4. 需求分析与功能映射

| 编号 | 功能需求 | 推荐页面 | 数据来源/实现方式 | 不可用处理 |
| --- | --- | --- | --- | --- |
| R1 | 时速 | 总览页、简洁页、导航页 | IMU 估算速度：加速度计/线性加速度计 + 陀螺仪/旋转向量联合解算，单位 m/s | 传感器缺失、未校准或漂移过大时显示“IMU 速度估算不可用/需重置” |
| R2 | 档位 | 总览页、简洁页 | 用户在 UI 中手动切换 P/R/N/D/S | 无硬件数据源，标注“模拟档位” |
| R3 | 电量/油量 | 总览页、简洁页 | 手机电池电量，`ACTION_BATTERY_CHANGED` 的 `EXTRA_LEVEL/EXTRA_SCALE` | 读取失败时显示“电量信息不可用” |
| R4 | 电机功率 | 总览页、简洁页 | `BatteryManager.BATTERY_PROPERTY_CURRENT_NOW` + 电压估算为 mW | 设备不支持电流时显示“实时功率不可用” |
| R5 | 转速/功率类指标 | 总览页、简洁页 | 无真实转速，建议显示“电机功率/系统负载”替代 | 标注为“课程演示指标” |
| R6 | G 值状态 | 总览页、传感器页 | `Sensor.TYPE_ACCELEROMETER`，m/s²，可除以 9.8 转为 G | 加速度计缺失时显示“加速度传感器不可用” |
| R7 | 陀螺仪 | 总览页、传感器页 | `Sensor.TYPE_GYROSCOPE`，rad/s | 陀螺仪缺失时显示“陀螺仪不可用” |
| R8 | 导航 | 导航页、总览页摘要 | 占位图/自绘路线卡片，不接入真实地图 | 显示“导航为演示模式” |
| R9 | 空调控制 | 空调页、总览页摘要 | 基础版本使用本地状态机 + `SharedPreferences` 持久化；扩展版本用 `ConsumerIrManager` 发送真实空调红外码 | 基础版本标注“本地空调状态”；扩展版本在红外缺失或码库未匹配时显示对应说明 |
| R10 | 多媒体控制 | 多媒体页 | `MediaSessionManager`/`MediaController` 联动系统活动媒体会话；简易控制可用 `AudioManager.dispatchMediaKeyEvent` | 未开启通知监听或无活动媒体会话时显示授权/不可用说明 |
| R11 | 车窗控制 | 控制页 | 本地 UI 演示状态 | 标注“演示控制” |
| R12 | 后视镜调节 | 控制页 | 本地 UI 演示状态 | 标注“演示控制” |
| R13 | 座椅加热/通风/按摩 | 控制页 | 本地 UI 演示状态 | 标注“演示控制” |
| R14 | 系统设置 | 设置页 | 通知监听授权、传感器、红外、数据源状态 | 不可用项提供说明 |

## 5. 导航与地图范围取舍

### 5.1 范围结论

本项目明确不接入真实地图 SDK，不使用 Google Maps、高德地图、百度地图或 OpenStreetMap。导航页只承担车机界面展示职责，用占位图、自绘路线或静态路线资源模拟导航视觉效果。

这样做可以避免 SDK Key、网络、隐私合规、地图生命周期、路线规划和定位权限等额外工作，把课程重点放在车机 UI、IMU 速度估算、系统媒体联动和本地控制状态上；红外空调控制留作扩展验证。

### 5.2 推荐方案

基础版本导航页设计为“演示导航卡片”：

- 中央为科技风格路线占位图，可使用静态图片、矢量图或自定义 View 绘制。
- 显示目的地名称、预计剩余距离、预计到达时间、路线进度和下一转向提示。
- 速度显示使用 IMU 估算速度，不依赖定位权限。
- 明确显示“导航为演示模式”，避免误解为真实路线导航。
- 可加入北向箭头、道路线条、路线进度和模拟车道提示增强车机观感。

## 6. UI 与页面设计

### 6.1 整体视觉方向

界面采用横屏车载驾驶舱风格，强调信息可读性而不是装饰复杂度：

- 主色调：深蓝、黑蓝、冷灰。
- 强调色：青蓝、冰蓝、少量绿色表示正常状态，橙/红表示警告。
- 背景：深色渐变或细微网格纹理，避免高对比花纹分散注意力。
- 卡片：半透明深色面板、细描边、轻微发光边缘。
- 字体层级：速度、档位、电量等核心信息最大；控制器状态次级；说明文字最小。
- 交互原则：少弹窗、少复杂层级，所有状态尽量在卡片内直接表达。

### 6.2 主界面结构

推荐主界面分为两块：

- 左侧固定标签栏：宽度约 80dp 到 112dp，包含图标和短文本。
- 右侧内容区：根据标签切换不同页面。

左侧标签建议包含：

1. 总览
2. 简洁
3. 导航
4. 传感器
5. 空调
6. 媒体
7. 控制
8. 设置

如果屏幕空间不足，可把“空调、媒体、控制”合并为“控制中心”页，但基础设计仍保留逻辑分区，避免所有组件堆在单页。

### 6.3 页面设计

#### 总览页

总览页用于展示驾驶舱核心状态：

- 大号速度仪表：当前 IMU 估算速度，单位 m/s。
- 档位卡片：P/R/N/D/S，支持用户点击切换。
- 电量卡片：手机电池百分比，作为车辆电量/油量模拟。
- 电机功率卡片：手机实时功率估算，单位 mW。
- G 值小卡：显示 X/Y/Z 或合成 G。
- 陀螺仪小卡：显示绕 X/Y/Z 轴角速度。
- 导航摘要：目的地、剩余距离、路线进度，标注“演示导航”。
- 空调摘要：温度、风量、模式。

#### 简洁车辆信息页

该页面只显示车辆信息，不显示多媒体或控制器：

- 速度、档位、电量、功率。
- 车况状态：正常、注意、警告三态。
- 传感器可用性概览。
- IMU 速度估算状态。
- 系统时间和课程演示模式标识。

该页适合作为“驾驶专注模式”，界面元素应比总览页更少，留白更多。

#### 导航页

基础版本包含：

- 路线占位图或自绘路线面板。
- 当前 IMU 估算速度，单位 m/s。
- 目的地模拟文本。
- 预计到达时间与剩余距离模拟。
- IMU 速度估算状态。
- 导航演示模式说明。

该页不接入真实地图，也不嵌入 `MapView` 或 `SupportMapFragment`。

#### 传感器页

传感器页用于展示手机板载传感器数据：

- 加速度计三轴数据，单位 m/s²。
- G 值换算结果。
- 陀螺仪三轴角速度，单位 rad/s。
- 传感器刷新时间戳。
- 传感器可用性说明。

每个传感器卡片底部必须有状态文案，例如“数据正常”“陀螺仪不可用”“传感器权限或硬件不可用”。

#### 空调页

空调页由本地状态机驱动：

- 温度调节：例如 16°C 到 30°C。
- 风量调节：0 到 5 档。
- 模式：自动、制冷、制热、除雾、送风。
- AC 开关。
- 内/外循环。
- 红外扩展状态：未接入、支持、不可用、发送中、发送失败。

基础版本即使不接入红外，空调页也应可用，并显示“当前为空调本地状态”。如果后续开启红外扩展，再显示“当前设备不支持红外发射”或具体发送结果。

#### 多媒体页

多媒体页优先联动 Android 系统当前活动媒体会话，而不是再实现一个独立播放器：

- 通过 `MediaSessionManager` 获取当前活动 `MediaController`。
- 使用 `MediaController.TransportControls` 执行播放/暂停、上一首、下一首。
- 显示当前媒体会话的曲目标题、艺术家、播放状态和进度。
- 简化控制可通过 `AudioManager.dispatchMediaKeyEvent()` 向系统当前媒体消费者发送媒体按键事件。

普通第三方 App 无法无条件枚举和控制所有媒体会话。`MediaSessionManager.getActiveSessions()` 通常需要系统级 `MEDIA_CONTENT_CONTROL` 权限，或用户在系统设置中手动开启本应用的通知监听权限。因此设置页需要提供“开启通知监听权限”的入口和状态说明；未授权时，多媒体页只显示授权引导和基础媒体按键控制。

#### 控制页

控制页包含无真实数据源的 UI 演示组件：

- 车窗开合：四个车窗独立进度或开关。
- 后视镜调节：左/右镜选择，上下左右调节。
- 座椅：加热、通风、按摩档位。
- 车灯/氛围灯演示开关。

这些控件需要明确属于演示状态，避免误导为真实车辆控制。

#### 设置页

设置页用于展示系统能力与调试状态：

- 通知监听授权状态。
- 加速度计可用性。
- 陀螺仪可用性。
- 红外发射器可用性。
- 电池电流读取可用性。
- IMU 速度估算状态。
- 系统媒体会话状态。
- 数据源说明：真实手机传感器、手机电池、系统媒体会话、本地演示状态；红外发射为扩展能力。

## 7. 数据来源与可用性处理

### 7.1 数据源设计原则

所有数据源统一输出“值 + 单位 + 可用性 + 说明文案”。UI 不直接调用 Android 系统 API，而是通过 Provider 或 Repository 获取状态，避免 Activity 中堆积硬件逻辑。

建议定义通用状态模型：

- `available`：数据是否可用。
- `value`：当前值。
- `unit`：单位。
- `message`：展示给用户的说明。
- `timestamp`：最后更新时间。

### 7.2 传感器数据

加速度计：

- API：`SensorManager.getDefaultSensor(Sensor.TYPE_ACCELEROMETER)`。
- 单位：m/s²。
- G 值：可用 `sqrt(x² + y² + z²) / 9.8` 估算合成 G。
- 不可用：`getDefaultSensor` 返回 `null` 时显示“加速度传感器不可用”。

陀螺仪：

- API：`SensorManager.getDefaultSensor(Sensor.TYPE_GYROSCOPE)`。
- 单位：rad/s。
- 不可用：`getDefaultSensor` 返回 `null` 时显示“陀螺仪不可用”。

传感器监听应跟随页面/Activity 生命周期，在前台注册，在暂停时注销，避免耗电和内存泄漏。

### 7.3 IMU 速度估算

本项目不使用 GPS 速度，也不申请定位权限。速度显示改为通过手机 IMU 做短时估算，单位为 m/s。

#### 7.3.1 数据来源

- 优先使用 `Sensor.TYPE_LINEAR_ACCELERATION` 获取已去除重力的线加速度，单位 m/s²。
- 同时使用 `Sensor.TYPE_GYROSCOPE` 或 `Sensor.TYPE_ROTATION_VECTOR` 估计手机姿态，尽量把设备坐标系转换到稳定参考坐标系。
- 如果没有 `TYPE_LINEAR_ACCELERATION`，可用 `Sensor.TYPE_ACCELEROMETER` 结合重力估计做高通/低通滤波，但精度更差。

#### 7.3.2 计算思路

- 启动或用户点击“校准”时，要求手机静止 1 到 3 秒，采集零偏。
- 每次传感器回调根据时间差 `dt` 对线加速度积分：`v = v + a * dt`。
- 速度值单位保持 m/s，不换算为 km/h，以符合课程要求。
- 当线加速度长期接近 0 且速度很小，可做零速修正，把速度归零以抑制漂移。
- 提供“重置速度”按钮，用户发现漂移时可以手动归零。

#### 7.3.3 必须说明的限制

IMU 速度不是 GPS 或真实车轮速度，误差会随时间积分快速累积。它适合课程演示“加速/减速趋势”和短时间速度估算，不适合长时间真实车速测量。UI 文案应使用“IMU 估算速度”或“短时估算速度”，不要写成“真实车速”。

不可用状态包括：缺少线性加速度传感器、缺少陀螺仪/旋转向量、校准失败、采样率过低、噪声过大或积分漂移明显。此时显示“IMU 速度估算不可用/需重置”。

### 7.4 电量与功率

电量来源为手机电池：

- 电量百分比：通过 `ACTION_BATTERY_CHANGED` sticky intent 读取 `EXTRA_LEVEL` 与 `EXTRA_SCALE`。
- 电流：通过 `BatteryManager.BATTERY_PROPERTY_CURRENT_NOW` 读取，单位通常为微安 uA。
- 功率：Android 没有直接提供 mW API，可在电流和电压都可用时估算：`功率 mW = 电流 uA * 电压 mV / 1,000,000`。

注意：电流和功率读数高度依赖设备硬件与系统实现，部分机型可能返回不可用值或数值噪声较大。因此 UI 应标注“手机功率估算”，不可作为真实车辆电机功率。

### 7.5 档位

档位没有真实数据源，由用户在 UI 中切换：

- 可选值：P、R、N、D、S。
- 状态保存在内存中，可选持久化到 `SharedPreferences`。
- UI 标注为“模拟档位”。

### 7.6 空调状态

空调状态内部维护本地状态机：

- `powerOn`：开关。
- `temperature`：温度。
- `fanLevel`：风量。
- `mode`：自动、制冷、制热、除雾、送风。
- `acEnabled`：AC 开关。
- `circulationMode`：内循环/外循环。

状态需要持久保存，推荐使用 `SharedPreferences`。基础版本只维护本地状态机；测试手机虽然支持红外，但真实空调红外控制应放入扩展版本：

- 使用 `PackageManager.FEATURE_CONSUMER_IR` 或 `ConsumerIrManager.hasIrEmitter()` 检查设备是否支持红外发射。
- 使用 `ConsumerIrManager.getCarrierFrequencies()` 确认发射频率范围，常见家电红外载波约 38kHz，但必须以设备支持范围和码库为准。
- `ConsumerIrManager.transmit(carrierFrequency, pattern)` 发送交替 on/off 的微秒级脉冲数组，且单次 pattern 必须短于 2 秒。
- 空调红外码不是简单“温度+1”按钮码。很多空调遥控器每次发送的是完整状态帧，包含开关、模式、温度、风量、扫风等全部状态以及校验位。
- 因此需要按空调品牌/型号准备红外码库，或实现对应协议编码器。例如先支持测试空调型号，再扩展格力、美的、海尔、奥克斯等品牌。
- 手机红外发射器通常只负责发送，不能像学习型遥控器一样直接接收并学习原遥控器波形；如果需要学习码，应借助外部红外接收器，或从第三方码库导入 raw、Pronto、LIRC、IRPlus 等格式。
- 每次用户修改空调状态后，由 `HvacState` 生成完整目标状态，再通过 `IrCodeRepository` 查找或生成对应 `carrierFrequency + pattern`，最后交给 `IrHvacController` 发送。
- 发送前显示待发送状态，发送后记录最近一次命令、发送时间和结果；如果码库缺失，显示“当前空调型号未配置红外码”。
- 每个品牌/型号配置应带“已验证”标记；未在测试空调上验证通过的码库不应作为默认选项。
- 红外发送不应在复杂计算时阻塞主线程；发送按钮也应做短时间防连点，避免连续发射导致空调状态错乱。

### 7.7 多媒体控制

基础版本优先联动手机系统当前媒体会话：

- 声明 `NotificationListenerService`，引导用户在系统设置中为本应用开启通知使用权。
- 开启后通过 `MediaSessionManager.getActiveSessions(ComponentName)` 获取当前活动媒体会话列表。
- 选择优先级最高或用户指定的 `MediaController`，读取包名、metadata、playback state 和 transport controls。
- 播放/暂停、上一首、下一首优先调用 `MediaController.getTransportControls()`。
- 如果未开启通知监听或没有可用 `MediaController`，可退化为 `AudioManager.dispatchMediaKeyEvent()` 发送 `KEYCODE_MEDIA_PLAY_PAUSE`、`KEYCODE_MEDIA_NEXT`、`KEYCODE_MEDIA_PREVIOUS` 给系统当前媒体消费者。
- UI 需要区分“已连接媒体会话”“仅媒体按键模式”“未发现媒体会话”三种状态。

普通应用不能无条件获得系统级 `MEDIA_CONTENT_CONTROL` 权限，因此“像系统媒体控件一样枚举并精确控制所有媒体 App”的前提是用户主动开启通知监听权限，或者被控媒体 App 主动提供可连接的媒体会话 token/service。

## 8. 架构设计与包结构

### 8.1 架构原则

本项目推荐保持单模块，但在包内做清晰分层：

- UI 只负责渲染和用户交互。
- Provider 负责 Android 系统 API 与硬件能力读取。
- Repository 负责状态存储和业务状态聚合。
- Model 使用普通 Java 类，方便本地单元测试。
- 工具类负责单位换算、格式化、权限状态转换。

不建议一开始引入多模块、复杂依赖注入框架或完整 MVVM 基建。课程项目应优先保证可完成、可演示、可测试。

### 8.2 推荐包结构

```text
com.example.vehicle_mountedsystem
├── MainActivity.java
├── ui
│   ├── MainShellController.java
│   ├── TabController.java
│   └── pages
│       ├── DashboardPageController.java
│       ├── MinimalVehiclePageController.java
│       ├── NavigationPageController.java
│       ├── SensorPageController.java
│       ├── HvacPageController.java
│       ├── MediaPageController.java
│       ├── VehicleControlPageController.java
│       └── SettingsPageController.java
├── model
│   ├── VehicleState.java
│   ├── GearState.java
│   ├── HvacState.java
│   ├── MediaState.java
│   ├── SensorReading.java
│   ├── BatteryStatus.java
│   ├── ImuSpeedState.java
│   └── AvailabilityStatus.java
├── data
│   ├── sensor
│   │   └── MotionSensorProvider.java
│   ├── battery
│   │   └── BatteryStatusProvider.java
│   ├── speed
│   │   └── ImuSpeedEstimator.java
│   ├── hvac
│   │   ├── HvacRepository.java
│   │   └── optional_ir
│   │       ├── IrCodeRepository.java
│   │       └── IrHvacController.java
│   └── media
│       ├── SystemMediaController.java
│       └── MediaNotificationListenerService.java
└── util
    ├── UnitFormatter.java
    ├── PermissionHelper.java
    └── AnimationHelper.java
```

### 8.3 页面组织方式

考虑当前项目还没有 Navigation 依赖，基础实现可以先使用一个 Activity + 多个 XML 区块/页面控制器：

- `MainActivity` 加载主 shell 布局。
- 左侧标签栏负责切换当前页面。
- 右侧内容区可以先使用多个 `ViewGroup` 切换显示/隐藏。
- 每个页面控制器绑定一个布局区域，负责渲染对应状态。

如果后续希望结构更规范，可以引入 AndroidX Navigation 与 Fragment：

- `MainActivity` 只保留 shell。
- 右侧内容区使用 `FragmentContainerView`。
- 页面拆成多个 Fragment。
- 使用共享 ViewModel 或 Activity 级状态对象管理跨页面数据。

两种方式都符合当前 Java/XML 项目基础。为了课程项目稳妥，建议先实现“单 Activity + 页面控制器”，后续再视时间迁移 Fragment。

## 9. 持久化、权限与依赖

### 9.1 持久化方案

基础版本使用 `SharedPreferences` 即可：

- 保存空调状态机。
- 保存上次选中的页面。
- 保存模拟档位。
- 保存座椅、车窗、后视镜等演示控制状态。

不建议基础版本引入数据库。只有在后续要记录传感器历史曲线、驾驶日志或多次行程数据时，才考虑 Room。

### 9.2 权限规划

基础版本根据实际功能逐步添加权限，不提前堆砌权限。

速度与导航不使用定位权限，也不接入地图 SDK，因此基础版本不申请 `ACCESS_FINE_LOCATION`、`ACCESS_COARSE_LOCATION`、`INTERNET` 或 `ACCESS_NETWORK_STATE`。

红外扩展相关：

- 基础版本不强制接入红外，也不需要为了红外提前增加权限或实现代码。
- 进入扩展版本后，使用 `ConsumerIrManager` 前先检查系统特性和 `hasIrEmitter()`。
- 如果目标 SDK 或设备要求额外红外权限，应按实际编译报错和官方文档补充。

通知/媒体相关：

- 声明 `NotificationListenerService` 需要在 manifest 中使用 `android.permission.BIND_NOTIFICATION_LISTENER_SERVICE`，并通过系统设置让用户手动开启通知使用权。
- 只有开启通知监听后，普通应用才能通过 `MediaSessionManager.getActiveSessions(ComponentName)` 枚举活动媒体会话。
- 不依赖系统级 `MEDIA_CONTENT_CONTROL` 权限，因为该权限通常不授予普通第三方应用。
- 基础版本不做前台媒体通知，因此不强制申请 `POST_NOTIFICATIONS`。

默认不申请后台定位权限，因为本项目不使用定位。

### 9.3 依赖规划

基础版本当前依赖基本够用：

- AppCompat：Activity 与主题兼容。
- Material：按钮、卡片、滑块等 UI 控件。
- ConstraintLayout：横屏复杂布局。
- JUnit/Espresso：测试基础。

可选依赖：

- AndroidX Lifecycle/ViewModel：跨页面共享状态。
- AndroidX Navigation：Fragment 页面导航。
- AndroidX Media 或 Media3：可辅助处理媒体会话/控制器兼容性，但基础系统媒体控制也可先用平台 `android.media.session` API。

原则：只有当功能进入实现阶段才添加依赖，避免文档和 Gradle 过早膨胀。

## 10. 动画与交互策略

### 10.1 页面切换动画

页面切换应轻量、快速、克制：

- 旧页面淡出，新页面轻微横向滑入并淡入。
- 左侧标签选中态使用指示条滑动或背景高亮。
- 页面切换时避免大幅旋转、闪烁或复杂粒子效果。

可使用 `ViewPropertyAnimator` 实现 `alpha`、`translationX` 和 `scale` 动画。若后续使用 Fragment，可使用 Fragment enter/exit 动画。

### 10.2 控件状态动画

控件状态变化需要有反馈：

- 速度数值变化：轻微缩放或数字滚动效果。
- 电量/功率变化：进度条平滑过渡。
- 档位切换：选中档位高亮滑动。
- 空调温度/风量：滑块或圆形控件平滑移动。
- 媒体播放/暂停：按钮图标淡入淡出。
- 车窗控制：进度条模拟升降。
- 后视镜调节：方向按钮点击有短暂高亮。

动画时长建议控制在 120ms 到 250ms，保持车机界面响应迅速。

### 10.3 性能约束

- 传感器数据刷新频率高，但 UI 不应每次传感器事件都全量重绘。
- 可将传感器 UI 更新限制在约 10Hz 内，IMU 速度估算内部可用更高采样率，但 UI 不必逐帧刷新。
- 动画只作用于必要控件，避免整个页面频繁重排。
- 若设备性能较弱，应优先保证文字与状态可读。

## 11. 测试与验证计划

### 11.1 TDD 优先对象

实现时优先为纯 Java 模型和工具类写本地单元测试：

- `HvacState`：温度上下限、风量上下限、模式切换、开关状态。
- `GearState`：档位切换合法性、显示文本。
- `UnitFormatter`：IMU 速度 m/s 格式化、uA/mV 到 mW、G 值换算。
- `AvailabilityStatus`：传感器缺失、权限缺失、数据未知时的文案。
- `MediaState`：活动媒体会话、播放/暂停、上一首、下一首、授权状态。
- `ImuSpeedState`：零偏校准、速度积分、零速修正、重置速度。
- 扩展红外测试：`IrCodeRepository` 按品牌/型号/空调状态查找红外码，缺失时返回不可用状态。
- `BatteryStatus`：电量百分比计算、电流不可用处理。

### 11.2 instrumentation 与手动验证

设备或模拟器验证重点：

- App 横屏启动和布局适配。
- 左侧标签栏能切换页面。
- 页面切换有动画。
- IMU 速度未校准或漂移过大时，速度/导航组件显示说明且不崩溃。
- 模拟器或缺少陀螺仪设备上，传感器页显示“陀螺仪不可用”。
- 电量百分比可以显示。
- 电流/功率不可用时显示说明。
- 空调状态修改后，重启 Activity 仍能恢复。
- 基础版本未接入红外时，空调页显示“本地空调状态”；扩展版本再验证测试空调码库匹配和真实发送。
- 开启通知监听后，多媒体按钮能控制系统当前活动媒体会话。
- 未开启通知监听时，多媒体页显示授权引导或退化为媒体按键模式。

### 11.3 验证命令

常用命令：

```bash
./gradlew :app:assembleDebug
./gradlew :app:testDebugUnitTest
./gradlew :app:lintDebug
./gradlew :app:connectedDebugAndroidTest
```

其中 `connectedDebugAndroidTest` 需要连接设备或启动模拟器。

## 12. 里程碑计划

### M1：设计文档与范围确认

- 完成 `DESIGN.md`。
- 明确基础版本与可选扩展。
- 确认默认导航采用占位方案。

### M2：静态横屏 UI 框架

- 主界面改为横屏驾驶舱布局。
- 实现左侧标签栏。
- 创建总览、简洁、导航、传感器、空调、媒体、控制、设置页面的静态布局。

### M3：核心状态模型与单元测试

- 实现 `HvacState`、`GearState`、`MediaState`、`ImuSpeedState`、`AvailabilityStatus`、`UnitFormatter`。
- 为核心状态转换和单位换算补充 JUnit 测试。

### M4：真实手机数据接入

- 接入电量读取。
- 接入加速度计、线性加速度计、陀螺仪/旋转向量。
- 完成 IMU 速度估算、静止校准、零速修正和重置按钮。
- 完成传感器不可用和 IMU 漂移/未校准 UI。

### M5：控制器功能接入

- 完成空调本地状态机和持久化。
- 完成系统媒体会话发现、通知监听授权引导和媒体控制联动。
- 完成车窗、后视镜、座椅演示状态。

### M6：动画与视觉打磨

- 页面切换动画。
- 标签栏选中动画。
- 核心数值与控件状态变化动画。
- 统一冷色科技风样式。

### M7：红外扩展联调

- 使用测试手机和真实空调验证红外开关、模式、温度、风量命令。
- 完成红外码缺失、设备不支持、连续发送过快等降级状态。
- 该阶段为可选扩展，不阻塞基础版本验收。

### M8：最终测试与展示材料

- 运行构建、单元测试、Lint。
- 在设备或模拟器上完成手动验收。
- 准备课程演示脚本、截图和功能说明。

## 13. 风险与应对

| 风险 | 影响 | 应对方案 |
| --- | --- | --- |
| 模拟器缺少陀螺仪或加速度计 | 传感器页无数据 | 必须显示不可用文案，可准备真机演示 |
| IMU 积分漂移 | 速度估算会随时间偏离真实值 | 标注“IMU 估算速度”，提供静止校准、零速修正和重置按钮 |
| 线性加速度/陀螺仪不可用 | 无法进行速度估算 | 显示“IMU 速度估算不可用”，保留其他车辆信息 |
| 电流/功率 API 不被设备支持 | 电机功率无法显示真实值 | 显示“实时功率不可用”，可用演示值但必须标注 |
| 红外扩展码库不匹配测试空调 | 扩展空调命令无效或状态错乱 | 不影响基础版本；扩展时先确定测试空调品牌/型号，优先实现该型号完整状态帧 |
| 红外扩展连续发射过快 | 扩展空调状态可能不同步 | 不影响基础版本；扩展发送按钮防连点，记录最近一次命令和发送结果 |
| 未开启通知监听权限 | 无法枚举系统活动媒体会话 | 设置页提供授权入口，多媒体页显示授权引导或退化为媒体按键模式 |
| 无活动媒体会话 | 多媒体页无法读取曲目信息 | 显示“未发现活动媒体会话”，提示先打开音乐 App 播放一次 |
| UI 组件过多导致拥挤 | 不符合车载简洁要求 | 使用多页面和简洁页，控制单页信息密度 |
| 动画过度花哨 | 分散驾驶场景注意力 | 使用短时长、低幅度、功能导向动画 |

## 14. 后续扩展方向

如果基础版本稳定，可以继续扩展：

- 扩展更多空调品牌/型号红外码库。
- 增加红外码导入/导出或从已有遥控器码库选择。
- 引入 Media3 或 AndroidX Media 兼容层，增强不同媒体 App 的会话适配。
- 增加 IMU 速度和加速度历史曲线。
- 增加夜间/白天主题切换。
- 增加语音播报或语音命令演示。
- 增加导航占位图主题切换，但仍不接入真实地图。

## 15. 总体结论

本项目最稳妥的课程实现路线是：先完成横屏车机 UI、多页面切换、车辆状态可视化、手机传感器数据、电池状态、IMU 速度估算、本地空调状态机和系统媒体会话联动；导航页只做演示占位，不接入真实地图。真实红外空调控制作为扩展能力，不阻塞基础版本验收；完整真实导航、真实车辆 CAN/ECU 接入和无授权控制所有媒体 App 都不应作为基础版本承诺。

该方案能覆盖老师要求的车辆状态、车载控制、辅助功能和科技感横屏界面，也能满足个人希望的分页面、简洁页、传感器数据源和动画要求，同时把硬件不可用、权限缺失、地图工作量等风险控制在课程项目可完成范围内。
